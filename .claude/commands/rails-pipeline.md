Read `4-SYSTEM/Skills/rails-pipeline/SKILL.md` in full, then execute it on the file(s) or goal the user specifies.

Skill purpose: Plans, runs, or resumes the rails pipeline for a text — choosing which skills to run, in what order, with each skill's completion check and the human gates between them.
